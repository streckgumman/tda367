package model;

/**
 * Enumerable containing types for GameObjects. Each GameObject (except for player) has a type,
 * specified by which GameObjectType it is.
 */
public enum GameObjectType {
    PLAYER,
    KEY,
    SCISSORS,
    HAMMER,
    DOGWITHGUN,
    DOOR,
    BUSH,
    ROCK,
}
